name: bestmnist
dataset: mnist

dataset_args:
  rotation: 15
  min_rotation: 8
  rot_variants: 5


optimizer:
  type: adam
  learning_rate: 0.05


layers:
  - type: input

  - type: hidden
    dropout: 0.3
    nodes: 1024
    activation: leakyrelu

  - type: output
    nodes: 10
    activation: sigmoid
    loss: onehot
    metric: accuracy

weight: he
epochs: 1000
valid_freq: 1
batch_size: 512
